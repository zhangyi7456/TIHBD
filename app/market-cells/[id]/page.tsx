import { notFound } from "next/navigation"; import Link from "next/link"; import { ArrowLeft } from "lucide-react";
import { getIndustry,getMarketCell,marketCells } from "@/lib/data"; import { ScoreBar } from "@/components/score-bar"; import { ConfidenceMeter } from "@/components/confidence-meter"; import { GatekeeperFlow } from "@/components/gatekeeper-flow"; import { UncertaintyBand } from "@/components/uncertainty-band";
export function generateStaticParams(){return marketCells.map(x=>({id:x.id}))}
const labels={price:"价格惩罚",credit:"账期惩罚",quality:"好货获得惩罚",information:"信息惩罚",liquidation:"变现惩罚"};
export default async function MarketCellDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const cell=getMarketCell(id);if(!cell)notFound();const industry=getIndustry(cell.industryId);if(!industry)notFound();const half=Math.round(25*(1-cell.confidence));const estimate=Math.round(cell.eeb);return <main className="shell pt-10 fade-in"><Link href="/market-cells" className="text-xs flex items-center gap-2 text-[color:var(--muted)]"><ArrowLeft size={13}/> 返回市场单元</Link><section className="mt-10 grid lg:grid-cols-[1fr_420px] gap-14 border-b hairline pb-12"><div><p className="eyebrow">{industry.sector} · {industry.name} · {cell.period}</p><h1 className="serif text-5xl md:text-7xl mt-4">{cell.region}<br/><i className="font-normal text-[color:var(--accent)]">{cell.stage}</i></h1><p className="mt-7 text-[color:var(--muted)]">{cell.note}</p><span className="prior mt-7">研究假设 · 仅适用于该市场单元</span></div><div className="border-l hairline pl-8 self-end"><UncertaintyBand estimate={estimate} lower={Math.max(0,estimate-half)} upper={Math.min(100,estimate+half)} status="证据不足"/><div className="grid grid-cols-2 gap-6 mt-8"><div><span className="eyebrow">Outsider penalty</span><b className="block serif text-4xl">{Math.round(cell.op)}</b></div><div><span className="eyebrow">Prior contribution</span><b className="block serif text-4xl">{Math.round((1-cell.confidence)*100)}%</b></div></div><div className="mt-7"><ConfidenceMeter value={cell.confidence}/></div></div></section><section className="py-12 grid lg:grid-cols-2 gap-14"><div><p className="eyebrow">Barrier fingerprint</p><h2 className="serif text-3xl mt-3 mb-7">有效进入条件</h2><div className="space-y-5"><ScoreBar label="RBI" value={cell.rbi}/><ScoreBar label="SBI" value={cell.sbi}/><ScoreBar label="CBI" value={cell.cbi}/><ScoreBar label="BPI" value={cell.bpi} accent/></div></div><div><p className="eyebrow">Outsider penalty</p><h2 className="serif text-3xl mt-3 mb-7">圈外人惩罚结构</h2><div className="space-y-5">{Object.entries(cell.outsiderPenalty).map(([key,value])=><ScoreBar key={key} label={labels[key as keyof typeof labels]} value={value}/>)}</div><p className="text-xs text-[color:var(--muted)] mt-6">OP 为研究先验：价格 15%、信用 25%、质量 20%、信息 20%、变现 20%。</p></div></section><section className="border-t hairline py-12"><p className="eyebrow">Gatekeeper migration</p><h2 className="serif text-3xl mt-3">入口权力如何迁移</h2><GatekeeperFlow oldGatekeeper={industry.oldGatekeeper} newGatekeeper={industry.newGatekeeper}/></section><section className="border-t hairline py-12 grid md:grid-cols-2 gap-12"><div><p className="eyebrow">Measurement plan</p><h2 className="serif text-3xl mt-3">下一步如何验证</h2></div><ol className="space-y-4 text-sm text-[color:var(--muted)]"><li>01 · 固定规格、数量与付款方式进行多人询价。</li><li>02 · 记录价格、账期、质量、最小采购量和交付条件。</li><li>03 · 区分陌生买家、同行介绍和长期采购身份。</li><li>04 · 将结果绑定到本单元，而不是推广到全国行业。</li></ol></section></main>}
