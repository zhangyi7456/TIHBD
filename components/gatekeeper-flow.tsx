import { ArrowRight } from "lucide-react";
export function GatekeeperFlow({oldGatekeeper,newGatekeeper}:{oldGatekeeper:string;newGatekeeper:string}){const nodes=["分散供给",oldGatekeeper,"交易入口",newGatekeeper,"下游需求"];return <div className="overflow-x-auto py-4"><div className="flex items-center min-w-[760px]">{nodes.map((node,i)=><div key={`${node}-${i}`} className="contents"><div className={`flex-1 border-t-2 pt-4 min-h-20 ${i===1?"border-[color:var(--accent)]":i===3?"border-[color:var(--green)]":"border-black"}`}><span className="eyebrow">{i===1?"OLD":i===3?"EMERGING":`0${i+1}`}</span><b className="block mt-2 text-sm">{node}</b></div>{i<nodes.length-1&&<ArrowRight size={16} className="mx-4 shrink-0 text-[color:var(--muted)]"/>}</div>)}</div></div>}
