export function ScoreBar({label,value,accent=false}:{label:string;value:number;accent?:boolean}){return <div className="grid grid-cols-[minmax(120px,155px)_1fr_34px] items-center gap-3 text-xs"><span className="text-[color:var(--muted)]">{label}</span><div className="h-1.5 bg-black/8 overflow-hidden"><div className="h-full transition-all duration-500" style={{width:`${value}%`,background:accent?"var(--accent)":"var(--ink)"}}/></div><b className="text-right">{Math.round(value)}</b></div>}
