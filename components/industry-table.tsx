"use client";
import Link from "next/link"; import { useMemo,useState } from "react"; import type { IndustryWithEEB } from "@/lib/data"; import { Search,ArrowUpRight } from "lucide-react"; import { PriorLabel } from "./prior-label";
export function IndustryTable({items}:{items:IndustryWithEEB[]}){const [q,setQ]=useState("");const [cat,setCat]=useState("全部");const [sort,setSort]=useState<"eeb"|"bpi">("eeb");const cats=["全部",...new Set(items.map(x=>x.category))];const shown=useMemo(()=>items.filter(x=>(cat==="全部"||x.category===cat)&&(`${x.name}${x.mechanism}`.includes(q))).sort((a,b)=>b[sort]-a[sort]),[items,q,cat,sort]);return <section><div className="border-y hairline py-4 flex flex-wrap gap-3 items-center"><label className="flex items-center gap-2 border-b hairline px-1 py-2 min-w-60"><Search size={15}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="搜索行业或机制" className="bg-transparent outline-none w-full"/></label><select value={cat} onChange={e=>setCat(e.target.value)} className="bg-transparent border-b hairline px-2 py-2">{cats.map(x=><option key={x}>{x}</option>)}</select><div className="ml-auto flex gap-1 text-xs"><button onClick={()=>setSort("eeb")} className={`px-3 py-2 ${sort==="eeb"?"bg-black text-white":""}`}>按 EEB</button><button onClick={()=>setSort("bpi")} className={`px-3 py-2 ${sort==="bpi"?"bg-black text-white":""}`}>按 BPI</button></div></div><div className="text-xs text-[color:var(--muted)] py-3 flex justify-between"><span>{shown.length} 个研究单元</span><PriorLabel/></div><div className="overflow-x-auto"><table className="w-full border-collapse text-sm"><thead className="text-left text-[11px] uppercase tracking-wider text-[color:var(--muted)]"><tr className="border-b hairline"><th className="py-3">行业</th><th>分类</th>{["RBI","SBI","CBI","BPI","EEB"].map(x=><th key={x} className="text-right px-2">{x}</th>)}<th className="pl-8">核心机制</th><th/></tr></thead><tbody>{shown.map(x=><tr key={x.id} className="border-b hairline hover:bg-white/45 transition-colors group"><td className="py-4 font-medium min-w-48"><Link href={`/industry/${x.id}`}>{x.name}</Link></td><td className="text-xs text-[color:var(--muted)] min-w-24">{x.category}</td>{[x.rbi,x.sbi,x.cbi,x.bpi].map((v,i)=><td key={i} className="text-right tabular-nums px-2">{v}</td>)}<td className="text-right tabular-nums px-2 font-bold text-[color:var(--accent)]">{Math.round(x.eeb)}</td><td className="pl-8 text-xs text-[color:var(--muted)] min-w-72">{x.mechanism}</td><td><Link href={`/industry/${x.id}`}><ArrowUpRight size={15} className="opacity-0 group-hover:opacity-100"/></Link></td></tr>)}</tbody></table></div></section>}
